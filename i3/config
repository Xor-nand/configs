# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

	# custom color scheme inspired by the ARC-DARK theme

	set $bg-color		       #2f343f
	set $inactive-bg-color	  #2f343f
	set $text-color		      #f3f4f5
	set $inactive-text-color	 #676E7D
	set $urgent-bg-color	#E53935
	client.focused                   $bg-color                     $bg-color                    $text-color                   #3f454f
	client.unfocused              $iancative-bg-color    $inactive-bg-color     $inactive-text-color    #2f343f
	client.focused_inactive   $iancative-bg-color    $inactive-bg-color     $inactive-text-color    #2f343f
	client.urgent                     $urgent-bg-color        $urgent-bg-color      $text-color                   #2f343f

# Window border size
new_window pixel 4
new_float pixel 4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango: Noto Mono  15  ##another font is set in the bar block

# This font is my personal go-to, you can choose another one
# text rendering and scalability on retina/hidpi displays (thanks to pango).
font pango: Noto Mono 15

# Use Mouse $mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal, if you use a different terminal emulator write that instead of termite.
#termite is an highly customizable terminal emulator for linux
bindsym $mod Return exec termite

# kill focused window / close application in focused window
bindsym $mod q kill

# start rofi (a program launcher) CURRENTLY NOT CONFIGURED
# an older version of rofi is customizable from launch command, if you have that version use the commented command below instead of the uncommented one
# bindsym $mod d exec rofi -show run -hide-scrollbar -bw 0 -lines 5 -eh 2 rofi -font "Noto Mono 11" -color-normal "$bg-rofi  , $other , $bg-rofi  , $bg-rofi  , $text-rofi "
# bindsym $mod d exec rofi -show run -hide-scrollbar -bw 0 -lines 7 -eh 2 -font "Noto Mono 11"
bindsym $mod d exec rofi -show run -hide-scrollbar -bw 0 -lines 5 -eh 1 -font "Noto Mono 11"
# Personalized arrowkeys, using i, k, j, l as up, down, left and right

# change focus
bindsym $mod j focus left
bindsym $mod k focus down
bindsym $mod i focus up
bindsym $mod l focus right

# alternatively, you can use the cursor keys:
bindsym $mod Left focus left
bindsym $mod Down focus down
bindsym $mod Up focus up
bindsym $mod Right focus right

# move focused window
bindsym $mod Shift j move left
bindsym $mod Shift k move down
bindsym $mod Shift i move up
bindsym $mod Shift l move right

# alternatively, you can use the cursor keys:
bindsym $mod Shift Left move left
bindsym $mod Shift Down move down
bindsym $mod Shift Up move up
bindsym $mod Shift Right move right


# split in horizontal orientation
bindsym $mod h split h


# split in vertical orientation
bindsym $mod v split v


# enter fullscreen mode for the focused container
bindsym $mod f fullscreen toggle



# change container layout (stacked, tabbed, toggle split)
bindsym $mod s layout stacking
bindsym $mod w layout tabbed
bindsym $mod e layout toggle split


# toggle tiling / floating
bindsym $mod Shift space floating toggle


# change focus between tiling / floating windows
bindsym $mod space focus mode_toggle


# focus the parent container
bindsym $mod a focus parent


# focus the child container
#bindsym $mod d focus child


# switch to workspace
##personalized variables for workspaces

set $works1 " 1 "
set $works2 " 2 "
set $works3 " 3 "
set $works4 " 4 "
set $works5 " 5 "
set $works6 " 6 "
set $works7 " 7 "
set $works8 " 8 "
set $works9 " 9 "
set $works10 " X "

# switch to workspace
bindsym $mod 1 workspace $works1
#workspace $works11
#workspace $works1 output eDP1
#workspace $works11 output DP1
bindsym $mod 2 workspace $works2
#workspace $works2 output eDP1
#workspace $works22 output DP1
bindsym $mod 3 workspace $works3
#workspace $works33
#workspace $works3 output eDP1
#workspace $works33 output DP1
bindsym $mod 4 workspace $works4
#workspace $works4 output eDP1
#workspace $works44 output DP1
bindsym $mod 5 workspace $works5
#workspace $works5 output eDP1
#workspace $works55 output DP1
bindsym $mod 6 workspace $works6
#workspace $works6 output eDP1
#workspace $works66 output DP1
bindsym $mod 7 workspace $works7
#workspace $works77
#workspace $works7 output eDP1
#workspace $works77 output DP1
bindsym $mod 8 workspace $works8
#workspace $works8 output eDP1
#workspace $works88 output DP1
bindsym $mod 9 workspace $works9
#workspace $works9 output eDP1
#workspace $works99 output DP1
bindsym $mod 0 workspace $works10
#workspace $works10 output DP1


# move focused container to workspace
bindsym $mod Shift 1 move container to workspace $works1
bindsym $mod Shift 2 move container to workspace $works2
bindsym $mod Shift 3 move container to workspace $works3
bindsym $mod Shift 4 move container to workspace $works4
bindsym $mod Shift 5 move container to workspace $works5
bindsym $mod Shift 6 move container to workspace $works6
bindsym $mod Shift 7 move container to workspace $works7
bindsym $mod Shift 8 move container to workspace $works8
bindsym $mod Shift 9 move container to workspace $works9
bindsym $mod Shift 0 move container to workspace $works10


##personalized predefined workspace for apps

assign [class="Clementine"] $works10
#assign [class="TelegramDesktop"] $works5
#assign [class="Firefox"] $works3
#assign [class="Atom"] $works2

for_window [class="Pavucontrol"] floating enable
for_window [class="Lxappearance"] floating enable
#for_window [class="Termite"] floating enable
for_window [class="Thunar"]  floating enable
for_window [class='feh']  floating enable
for_window [class='Feh']  floating enable
for_window [class='TelegramDesktop']  floating enable
for_window [class='Virtualbox']  floating enable
for_window [class='virtualbox']  floating enable
for_window [class='vlc']  floating enable
for_window [class='Vlc']  floating enable



# reload the configuration file
#bindsym $mod Shift c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod Shift r restart
# exit i3 (logs you out of your X session)
bindsym $mod Shift e exec "i3-nagbar -t warning -m 'ARE YOU LEAVING ME, BRO ?  you will be transfered to a sad and empty terminal .' -b 'Gbye Xor' 'i3-msg exit'"


# resize window (you can also use the mouse for that)

mode "resize" {

	# These bindings trigger as soon as you enter the resize mode

	# Pressing left will shrink the window’s width.
	# Pressing right will grow the window’s width.
	# Pressing up will shrink the window’s height.
	# Pressing down will grow the window’s height.
	bindsym j resize shrink width 5 px or 5 ppt
	bindsym k resize grow height 5 px or 5 ppt
	bindsym i resize shrink height 5 px or 5 ppt
	bindsym l resize grow width 5 px or 5 ppt

	# same bindings, but for the arrow keys
	bindsym Left resize shrink width 5 px or 5 ppt
	bindsym Down resize grow height 5 px or 5 ppt
	bindsym Up resize shrink height 5 px or 5 ppt
	bindsym Right resize grow width 5 px or 5 ppt

	# back to normal: Enter or Escape
	bindsym Return mode "default"
	bindsym Escape mode "default"
}


#resize
bindsym $mod r mode "resize"


# thin borders
hide_edge_borders both


# sticky window
bindsym $mod Shift s sticky toggle


# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {

	status_command i3blocks -c ~/.config/i3/i3blocks.conf
	#status_command i3status
	tray_output primary
	position top
	font pango: Noto Mono 9

	colors {
	background $bg-color

		separator #757575
		#				border			background		text
		focused_workspace           $bg-color          	$bg-color		$text-color
		inactive_workspace            $inactive-bg-color    $inactive-bg-color	$inactive-text-color
		urgent_workspace              $urgent-bg-color     $urgent-bg-color	$text-color
		}

}

gaps inner  15
gaps outer  0

bindsym $mod n gaps inner current plus 5
bindsym $mod m gaps inner current minus 5

bindsym $mod Shift n gaps outer current plus 5
bindsym $mod Shift m gaps outer current minus 5

bindsym $mod b gaps inner all set 15; gaps outer all set 5

bindsym $mod Shift f exec firefox
bindsym $mod Shift o exec --no-startup-id /home/xor/Downloads/tor-browser_en-US/start-tor-browser.desktop
bindsym $mod Shift p exec pavucontrol
bindsym $mod Shift y exec lxappearance
bindsym $mod Shift t exec thunar
bindsym $mod Shift z exec scrot -b  'screen_%Y-%m-%d--%H:%M:%S.png' -e 'mv $f /home/xor/media/screens/'

# personalized i3 lock
bindsym $mod z exec locker

#exec clementine
#exec atom
#exec /home/tommaso/Downloads/Telegram/Telegram
#exec firefox
#exec_always setxkbmap -layout "us" -option "grp:alt_shift_toggle"
#exec thunar
exec compton -f
#exec dhcpcd enp1s0
#exec --no-startup-id /home/xor/tor-browser_en-US/start-tor-browser.desktop
exec --no-startup-id udiskie
exec_always feh --bg-fill /home/xor/media/pics/current_papes/home.jpg
exec_always xrandr --output HDMI2 --off --output HDMI1 --off --output DP1 --mode 1280x1024 --pos 1920x0 --rotate normal --output eDP1 --mode 1920x1080 --pos 0x0 --rotate normal --output VIRTUAL1 --off
exec nm-applet
exec conky

# Pulse Audio controls
#bindsym $mod Shift b exec --no-startup-id pactl set-sink-volume 0 +5% #increase sound volume
#bindsym $mod Shift n exec --no-startup-id pactl set-sink-volume 0 -5% #decrease sound volume
bindsym $mod Shift b exec --no-startup-id pactl set-sink-mute 0 toggle # mute sound

# Sreen brightness controls
bindsym  $mod Shift v exec xbacklight -inc 20 # increase screen brightness
bindsym $mod Shift c exec xbacklight -dec 20 # decrease screen brightness

# Media player controls
#bindsym $mod Shift v exec playerctl play
#bindsym $mod Shift c exec playerctl pause
#bindsym $mod Shift comma exec playerctl next
#bindsym $mod Shift dot exec playerctl previous
